// Placeholder — populated in commit 16 with the CSV import wizard.
export function ImportPage() {
  return (
    <section>
      <h1 className="text-xl font-semibold tracking-tight">Import</h1>
      <p className="mt-2 text-sm text-neutral-500">CSV wizard lands in commit 16.</p>
    </section>
  );
}
