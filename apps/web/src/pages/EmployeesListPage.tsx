// Placeholder — populated in commit 14 with the full list, filters,
// search, sort, and currency switcher.
export function EmployeesListPage() {
  return (
    <section>
      <h1 className="text-xl font-semibold tracking-tight">Employees</h1>
      <p className="mt-2 text-sm text-neutral-500">
        Paginated list lands in the next commit.
      </p>
    </section>
  );
}
